<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:munit="http://www.mulesoft.org/schema/mule/munit"
      xmlns:munit-tools="http://www.mulesoft.org/schema/mule/munit-tools"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/munit http://www.mulesoft.org/schema/mule/munit/current/mule-munit.xsd
        http://www.mulesoft.org/schema/mule/munit-tools http://www.mulesoft.org/schema/mule/munit-tools/current/mule-munit-tools.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd">

    <munit:config name="health-test-suite.xml"/>

    <http:request-config name="HTTP_Health_Request_config">
        <http:request-connection host="localhost" port="${http.port}"/>
    </http:request-config>

    <!-- Test: Health endpoint returns 200 with status UP and timestamp -->
    <munit:test name="health-endpoint-returns-200-with-status-up" description="GET /health returns 200 with status UP and ISO8601 timestamp">
        <munit:enable-flow-sources>
            <munit:enable-flow-source value="health-check-flow"/>
        </munit:enable-flow-sources>
        <munit:execution>
            <http:request config-ref="HTTP_Health_Request_config" method="GET" path="/health">
                <http:response-validator>
                    <http:success-status-code-validator values="1..999"/>
                </http:response-validator>
            </http:request>
        </munit:execution>
        <munit:validation>
            <munit-tools:assert-that expression="#[attributes.statusCode]" is="#[MunitTools::equalTo(200)]"/>
            <munit-tools:assert-that expression="#[payload.status]" is="#[MunitTools::equalTo('UP')]"/>
            <munit-tools:assert-that expression="#[payload.timestamp]" is="#[MunitTools::notNullValue()]"/>
        </munit:validation>
    </munit:test>

</mule>
